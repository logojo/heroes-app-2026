# Heroes APP

## Levantar desarrollo

1. clonar repositorio
2. editar el archivo `.env` con las variables de entorno basado en el archivo `.env.templete`
3. Ejecutar `npm install`
4. Ejecutar `npm run dev`
